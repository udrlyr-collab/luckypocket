"use client";import{FormEvent,useState}from"react";import AppShell from"./AppShell";import{api,saveSession}from"../lib/api";
export default function AuthForm({mode}:{mode:"login"|"register"}){const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),body:any={email:f.get("email"),password:f.get("password")};if(mode==="register"){body.username=f.get("username");body.nickname=f.get("nickname")}try{const value=await api<any>(`/api/auth/${mode}`,{method:"POST",body:JSON.stringify(body)});saveSession(value);location.href="/"}catch(x:any){setError(x.message)}}return <AppShell><div style={{maxWidth:520,margin:"50px auto"}}><div className="page-head"><div><span className="eyebrow">ACCOUNT</span><h1>{mode==="login"?"로그인":"회원가입"}</h1><p>가상 경제 원장에 안전하게 접속합니다.</p></div></div><section className="panel"><form className="form" onSubmit={submit}><label>이메일<input name="email" type="email" required maxLength={254}/></label>{mode==="register"&&<div className="form-row"><label>사용자명<input name="username" minLength={3} maxLength={30} required/></label><label>닉네임<input name="nickname" minLength={2} maxLength={20} required/></label></div>}<label>비밀번호<input name="password" type="password" minLength={12} maxLength={128} required/></label>{error&&<div className="state error">{error}</div>}<button>{mode==="login"?"로그인":"계정 생성"}</button></form></section></div></AppShell>}

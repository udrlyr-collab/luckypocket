"use client";import{FormEvent,useEffect,useState}from"react";import AppShell from"./AppShell";import{api}from"../lib/api";export default function Settings(){const[d,setD]=useState<any>(),[toast,setToast]=useState("");useEffect(()=>{api("/api/settings").then(setD).catch(e=>setToast(e.message))},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const value=await api("/api/settings",{method:"PATCH",body:JSON.stringify({priceColorMode:f.get("priceColorMode"),locale:f.get("locale")})});setD(value);setToast("설정을 저장했습니다.")}catch(x:any){setToast(x.message)}}return <AppShell><div className="page-head"><div><span className="eyebrow">PREFERENCES</span><h1>설정</h1><p>상승·하락 색상과 표시 언어를 선택합니다.</p></div></div><section className="panel" style={{maxWidth:620}}>{!d?<div className="state">불러오는 중입니다.</div>:<form className="form" onSubmit={submit}><label>가격 색상<select name="priceColorMode" defaultValue={d.price_color_mode}><option value="korean">한국식: 상승 빨강·하락 파랑</option><option value="global">글로벌식: 상승 초록·하락 빨강</option></select></label><label>언어<select name="locale" defaultValue={d.locale}><option value="ko-KR">한국어</option><option value="en-US">English</option></select></label><button>저장</button></form>}</section>{toast&&<div className="toast" onClick={()=>setToast("")}>{toast}</div>}</AppShell>}

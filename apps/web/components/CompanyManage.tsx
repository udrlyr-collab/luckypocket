"use client";import{FormEvent,useEffect,useState}from"react";import AppShell from"./AppShell";import{api}from"../lib/api";export default function CompanyManage({id}:{id:string}){const[d,setD]=useState<any>(),[toast,setToast]=useState("");const load=()=>api(`/api/companies/${id}`).then(setD).catch(e=>setToast(e.message));useEffect(()=>{load()},[id]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),actionType=String(f.get("actionType")),amount=String(f.get("amount")??"");const body:any={idempotencyKey:crypto.randomUUID(),actionType};if(amount)body.amount=amount;if(actionType==="set_dividend")body.rateBps=Number(amount);try{await api(`/api/companies/${id}/actions`,{method:"POST",body:JSON.stringify(body)});setToast("경영 액션을 실행했습니다.");load()}catch(x:any){setToast(x.message)}}return <AppShell><div className="page-head"><div><span className="eyebrow">CORPORATE CONTROL</span><h1>{d?.name??"기업 경영"}</h1><p>지배권을 가진 사용자만 실행할 수 있습니다. 현재 체결가는 직접 변경하지 않습니다.</p></div></div><div className="grid"><section className="panel third"><h2>경영 액션</h2><form className="form" onSubmit={submit}><label>액션<select name="actionType"><option value="invest_rd">R&D 투자</option><option value="invest_marketing">마케팅</option><option value="invest_capex">설비 투자</option><option value="repay_debt">부채 상환</option><option value="borrow">차입</option><option value="set_dividend">배당률 설정</option><option value="cost_cutting">비용 절감</option><option value="replace_ceo">CEO 교체</option></select></label><label>금액 또는 bp<input name="amount" type="number" min="0"/></label><button>실행</button></form></section><section className="panel" style={{gridColumn:"span 8"}}><pre>{JSON.stringify(d,null,2)}</pre></section></div>{toast&&<div className="toast" onClick={()=>setToast("")}>{toast}</div>}</AppShell>}
